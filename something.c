#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char** argv){
  int val = atoi(argv[1]);
  if(val > 10){
    printf("Hello!");
  }else{
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
  }
  if(val > 20){
    printf("Hello!");
  }else{
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
    printf("Goodbye!");
  }
  return 0;
}